# Log: robust-tui-turn-lifecycle-quality-gate signal notes

**Date:** 2026-03-23
**Change:** `openspec/changes/robust-tui-turn-lifecycle-quality-gate`
**Goal:** Record concrete tracked-TUI signals observed while tightening the complex success-interrupt-success quality gate

---

## Scope

- Record exact on-screen signals seen in real complex captures.
- Prefer concrete sample IDs and capture roots over inferred behavior.
- For Claude active detection, do not rely on the footer working-summary line alone; use the spinner symbol in the latest-turn region as the active signal.

## Capture roots referenced

- Claude partial complex capture:
  `tmp/demo/shared-tui-tracking-demo-pack/authoring/claude/claude_success_interrupt_success_complex/capture-20260323T123329Z`
- Codex complex capture:
  `tmp/demo/shared-tui-tracking-demo-pack/authoring/codex/codex_success_interrupt_success_complex/capture-20260323T115828Z`

## Claude Code signals

### Active-turn spinner evidence

- `s000011` shows:
  `✽ Ionizing…`
- `s000012` shows:
  `✻ Ionizing…`
- `s000068` shows:
  `· Vibing…`
- `s000069` shows:
  `✢ Vibing…`

Meaning in tracker terms:

- These spinner-prefixed lines are current-turn active evidence for Claude.
- The rotating leading symbol is the reliable active cue.
- The changing verb text (`Ionizing…`, `Vibing…`) is helpful context but is not the authority by itself.
- The footer line `⏵⏵ bypass permissions on ...` remains visible in both ready and active samples, so it must not be used as the decisive active signal.

Operational decision recorded here:

- `wait_for_active` for Claude should require latest-turn spinner evidence (`thinking_line`) rather than a footer summary match.

### Settled success evidence

- `s000056` and `s000061` show:
  `● READY`

Meaning in tracker terms:

- `● <payload>` is the visible response block used as success/success-candidate evidence.
- Once settled, the following ready-draft prompt may appear while `last_turn.result` must reset to `none` when newer-turn authority becomes visible.

### Interrupted-ready evidence

- `s000075` shows:
  `⎿ Interrupted · What should Claude do instead?`

Meaning in tracker terms:

- This is the real interrupted-ready surface for Claude after `Esc`.
- It can appear while the next draft prompt is already visible below it.
- Interrupted detection therefore needs latest-turn-region scoping and should accept this one-shot visible signal without waiting for multiple identical stable polls.

### Ready-draft overlap evidence

- `s000069` shows the current prompt already contains:
  `❯ Now search this repository for files related to terminal recording ...`
- The previous turn is still visibly active above it:
  `✢ Vibing…`

Meaning in tracker terms:

- Claude can display overlapping active-turn output and a newer visible draft at the same time.
- The tracker must preserve `surface.editing_input=yes` for the current prompt instead of downgrading to `unknown` because older-turn activity is still visible.

## Codex signals

### Success evidence

- `s000050` shows:
  `• READY`
- `s000159` shows:
  `• RECOVERED`

Meaning in tracker terms:

- `• <payload>` is the Codex success marker for this fixture family.

### Interrupted-ready evidence

- `s000069` and `s000081` show:
  `■ Conversation interrupted - tell the model what to do differently`

Meaning in tracker terms:

- This is Codex interrupted-ready evidence.
- It may remain visible above a newer prompt, so stale interrupted transcript text must not override a later active or success turn once newer-turn authority appears.

## Resulting implementation guidance

- Claude active waits: watch the spinner symbol in the latest-turn region.
- Claude interrupted waits: accept the explicit interrupted-ready surface on first observation.
- Shared lifecycle reset: newer visible draft or newer active authority clears stale terminal `last_turn`.
- Complex fixture labels should continue distinguishing:
  `ready_success`, `ready_draft`, `active_draft`, `ready_interrupted`, and final `ready_success`.

## Follow-up from the successful Claude recapture

- Fresh successful complex capture:
  `tmp/demo/shared-tui-tracking-demo-pack/authoring/claude/claude_success_interrupt_success_complex/capture-20260323T124200Z`
- Claude `2.1.81 (Claude Code)` used additional spinner verbs such as `Frolicking…` and `Skedaddling…`, but the same rotating-symbol rule still held.
- The final `● RECOVERED` span initially failed to settle because the detector treated the native-installer footer advisory as a success blocker.
- Detector rule updated: the installer advisory is still noted as context, but it no longer suppresses a real stable response block.
