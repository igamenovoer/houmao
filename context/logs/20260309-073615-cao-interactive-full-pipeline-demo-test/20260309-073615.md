# Test Run: cao-interactive-full-pipeline-demo

**Date:** 2026-03-09
**Workspace:** `tmp/demo/cao-interactive-full-pipeline-demo/20260309-073615-810926Z/`
**Agent identity:** `AGENTSYS-alice`
**Terminal ID:** `5e43c916`

---

## Environment

| Item | Status |
|---|---|
| `tmux` | 3.4 — present |
| `cao-server` | present (`~/.local/bin/cao-server`) |
| Claude credentials | present (`tests/fixtures/agents/brains/api-creds/claude/personal-a-default/`) |
| CAO target | `http://127.0.0.1:9889` |

---

## Step-by-step Results

### 1. `launch_alice.sh -y`

**Result: ✅ PASS**

Session started cleanly, `state.json` written with `active: true`, identity canonicalized to `AGENTSYS-alice`.

### 2. `run_demo.sh inspect`

**Result: ✅ PASS**

Returned correct tmux attach and terminal log tail commands.

### 3. Turn 1 — `send_prompt.sh --prompt "Say hello from the interactive CAO demo."`

**Result: ✅ PASS**

- `exit_status: 0`
- `response_text`: `"Hello from the interactive CAO demo! Ready to assist with GPU kernel optimization work.\n\n* Deliberating…"`
- No parser anomalies that affected extraction.
- One minor anomaly: `unknown_version_floor_used` (detected version `2.1.71` unknown; floor preset `2.1.62` used). Non-fatal.

### 4. Turn 2 — `send_prompt.sh --prompt "Summarize the files you can see in this workspace."`

**Result: ⚠️ ANOMALY — wrong response extracted**

- `exit_status: 0` (pipeline did not error out)
- `response_text`: `"Hello from the interactive CAO demo! Ready to assist with GPU kernel optimization work."` — **identical to turn 1's response**, not a summary of workspace files.

Parser anomalies recorded in the turn artifact:

```json
[
  {
    "code": "unknown_version_floor_used",
    "details": { "provider": "claude", "requested": "2.1.71", "selected": "2.1.62" },
    "message": "Detected/overridden version is unknown; using floor preset"
  },
  {
    "code": "baseline_invalidated",
    "details": { "preset": "claude_shadow_v2", "provider": "claude" },
    "message": "Extraction retried with baseline reset after parser drift"
  }
]
```

Other diagnostics:
- `baseline_invalidated: true`
- `baseline_pos: 2950` (non-zero — prior terminal content was present when baseline was re-evaluated)
- `completion_source: "runtime_shadow_mode_full"` / `extraction_source: "runtime_shadow_mode_full"`

**Interpretation:** The shadow parser detected that the stored baseline position was stale ("parser drift") and retried extraction with a reset baseline. After the reset, it re-extracted the response from a position that overlapped with the turn 1 output, returning the old response text instead of the new one. The `baseline_pos: 2950` suggests the terminal had ~2950 bytes of prior content and the reset baseline landed at or before the turn 1 response.

### 5. `run_demo.sh verify`

**Result: ✅ PASS** (structural check only)

- `status: "ok"`, `turn_count: 2`
- Verification passed because the contract checks structural shape (two turns, `exit_status: 0`, non-empty `response_text`, unique identity) — not semantic content. The stale response in turn 2 passed this structural gate.

### 6. `stop_demo.sh`

**Result: ✅ PASS**

- `active: false`, `stale_session_tolerated: false`, `returncode: 0`

---

## Summary

The pipeline ran end-to-end without any hard failures. All commands returned exit code 0 and `verify` passed.

**The one notable anomaly:** on turn 2, the `claude_shadow` parser emitted a `baseline_invalidated` anomaly, retried with a reset baseline, and extracted the wrong (stale, turn 1) response text. The actual Claude reply to "Summarize the files you can see in this workspace." was not captured. This is a pre-existing shadow-parser baseline-drift issue, not a new regression introduced by this run.

**No code was modified.** This log records observed behavior only.

---

## Reproduction

```bash
cd <repo-root>
scripts/demo/cao-interactive-full-pipeline-demo/launch_alice.sh -y
scripts/demo/cao-interactive-full-pipeline-demo/run_demo.sh inspect
scripts/demo/cao-interactive-full-pipeline-demo/send_prompt.sh --prompt "Say hello from the interactive CAO demo."
scripts/demo/cao-interactive-full-pipeline-demo/send_prompt.sh --prompt "Summarize the files you can see in this workspace."
scripts/demo/cao-interactive-full-pipeline-demo/run_demo.sh verify
scripts/demo/cao-interactive-full-pipeline-demo/stop_demo.sh
```
