# server-api-smoke

Lightweight role package for maintained managed-agent API and passive-server compatibility checks.

Use this role when the agent should stay inside a copied dummy project and give short deterministic prompt responses for server launch, request, and stop verification.
