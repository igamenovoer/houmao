# Compatibility Profiles

Tracked Markdown agent profiles used by the Houmao-managed CAO compatibility surface live here.

Use `server-api-smoke.md` when a compatibility-only API check needs a lightweight, deterministic TUI worker profile without pulling in a broad engineering role.
