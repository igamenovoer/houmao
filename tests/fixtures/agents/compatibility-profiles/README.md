# Compatibility Profiles

Tracked Markdown agent profiles used by the Houmao-managed CAO compatibility surface live here.

Use `server-api-smoke.md` for the `houmao-server` managed-agent API live suite. It keeps the compatibility TUI worker lightweight and deterministic enough for direct API lifecycle checks.
