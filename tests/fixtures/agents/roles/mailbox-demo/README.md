# mailbox-demo

Lightweight role package for mailbox tutorial, runtime-contract, and direct-mail smoke coverage.

Use this role when the agent should stay focused on one bounded mailbox task inside a tiny copied dummy project instead of exploring a large repository.
