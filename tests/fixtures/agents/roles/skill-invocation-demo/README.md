# Skill Invocation Demo Role

Use this role when the scenario is a narrow skill-invocation probe in a copied dummy project.

This role is intentionally smaller than `mailbox-demo` and far smaller than the repository-scale GPU roles. It biases the launched agent toward one installed-skill action and away from mailbox workflows or broad repository discovery.
