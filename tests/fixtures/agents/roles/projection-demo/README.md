# projection-demo

Lightweight role package for interactive server-state prompt-and-observe demos.

Use this role when the agent should stay inside a tiny copied dummy project and respond to short operator prompts without broad repository discovery.
