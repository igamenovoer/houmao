Create exactly one file at `tmp/single-agent-gateway-wakeup-headless/current-time.md`.

Write the current UTC time as a single line in ISO 8601 format.

Do not write any other files.
After the file exists, mark this source mailbox message as read.
