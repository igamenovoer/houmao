# Operator Mail For {{TOOL}}

Create exactly one file at `{{OUTPUT_FILE_PATH}}`.

Write exactly this single line into the file:

`{{OUTPUT_FILE_CONTENT}}`

Do not write any other files.
Do not change the requested content.
After the file exists with the exact requested content, mark this source mailbox message as read.

