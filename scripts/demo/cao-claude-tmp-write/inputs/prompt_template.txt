You are running inside the agent-definition directory directory.

Create exactly one runnable Python file at `{{OUTPUT_FILE_REL}}` with this exact content:

```python
def main() -> None:
    print("{{SENTINEL}}")


if __name__ == "__main__":
    main()
```

Rules:
- Do not modify any tracked repository files.
- Only create/update `{{OUTPUT_FILE_REL}}`.
- Do not run git commands.

After writing the file, reply with exactly one short line that starts with `DONE:` and includes the file path.
Put only that reply line between these exact sentinel lines:
AGENTSYS_DEMO_RESPONSE_BEGIN
AGENTSYS_DEMO_RESPONSE_END
