Create exactly one file at `tmp/single-agent-mail-wakeup/system-info.md`.

Gather host system information that does not require sudo access.
Include concise plain-text lines for:
- hostname
- current user
- kernel and OS
- uptime
- CPU model
- memory summary
- disk usage summary
- primary IP addresses
- current UTC time

Do not use sudo.
Do not write any other files.
After the file exists, mark this source mailbox message as read.
