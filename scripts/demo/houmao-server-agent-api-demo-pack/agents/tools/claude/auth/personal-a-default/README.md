# Claude Personal-A Default

Create the local-only credential file for this pack at:

```text
agents/brains/api-creds/claude/personal-a-default/env/vars.env
```

Required keys:

- `ANTHROPIC_API_KEY` or
- `ANTHROPIC_AUTH_TOKEN`

Tracked non-secret files:

- `files/claude_state.template.json`
