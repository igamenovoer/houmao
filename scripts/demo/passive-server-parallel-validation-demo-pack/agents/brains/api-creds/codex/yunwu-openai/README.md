# Codex Yunwu OpenAI

Create the local-only credential file for this pack at:

```text
agents/brains/api-creds/codex/yunwu-openai/env/vars.env
```

Required keys:

- `OPENAI_API_KEY`

Optional keys:

- `OPENAI_BASE_URL`
- `OPENAI_ORG_ID`

Optional local file:

- `files/auth.json`
