# Mailbox Tutorial: Reply Instructions

Author a short reply that confirms the mailbox roundtrip is active.

- Keep the reply in the same thread as the original `mail send` result.
- Mention the parent `message_id` from that earlier send.
- Confirm that the original sender can now run `mail check`.
- Keep the tone concise so the mailbox and `chats.jsonl` remain easy to inspect.
