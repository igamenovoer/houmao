# Mailbox Tutorial: Reply Message

Confirmed. The mailbox roundtrip is active and this reply should stay in the same thread.

- Mention that the parent `message_id` came from the earlier `mail send` result.
- Confirm that the original sender can now run `mail check`.
