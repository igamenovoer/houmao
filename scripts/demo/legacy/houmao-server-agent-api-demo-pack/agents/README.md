# Demo-Owned Agent Assets

This demo pack owns the non-secret selector inputs required for direct `houmao-server` managed-agent API validation.

Local credential material belongs under `agents/brains/api-creds/` and is intentionally ignored by git.
