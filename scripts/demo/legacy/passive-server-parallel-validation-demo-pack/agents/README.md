# Demo-Owned Agent Assets

This demo pack owns the non-secret selector inputs required for Step 7 passive-server parallel validation against one shared runtime and registry root.

Local credential material belongs under `agents/brains/api-creds/` and is intentionally ignored by git.
