## 1. README System-Skills Sync

- [ ] 1.1 Add table rows for `houmao-agent-loop-pairwise` and `houmao-agent-loop-relay` to the README "System Skills: Agent Self-Management" table with operator-facing "what it enables" summaries derived from the shipped `SKILL.md` files at `src/houmao/agents/assets/system_skills/houmao-agent-loop-pairwise/SKILL.md` and `.../houmao-agent-loop-relay/SKILL.md`.
- [ ] 1.2 Update the README narrative sentence that currently claims "twelve packaged" or "all twelve" system skills so the count matches the fourteen-skill `src/houmao/agents/assets/system_skills/catalog.toml` and rewrite nearby auto-install wording so it describes the resolved `managed_launch_sets` / `managed_join_sets` / `cli_default_sets` contents instead of a frozen number (for example "everything except the lifecycle-only `houmao-agent-instance`" remains valid but the skill list behind it must include both loop skills).
- [ ] 1.3 Confirm the README "System Skills" paragraph that describes the twelve-skill default set reference list still matches the shipped auto-install sets, and replace the frozen "eleven of these twelve" wording with language that stays valid as the catalog grows.
- [ ] 1.4 Add `houmao-mgr credentials` to the README "CLI Entry Points" table (or to the neighboring paragraph that enumerates maintained entry points) and cross-link it to the new `credentials` section in the CLI reference.

## 2. Overview Guide System-Skills Sync

- [ ] 2.1 Update the "Packaged Skills" table in `docs/getting-started/system-skills-overview.md` so every skill declared in `catalog.toml` appears exactly once, including rows for `houmao-agent-loop-pairwise` and `houmao-agent-loop-relay`. Place the loop skills in the most natural existing concern subsection or introduce a short "Loop authoring and master-run control" subsection if needed to keep routing readable.
- [ ] 2.2 Rewrite the narrative sentence that currently says "**twelve** system skills" so the count matches the current catalog. Update the "four concern groups" framing so either every skill still fits one of the named groups, or the framing explicitly covers the loop skills.
- [ ] 2.3 Rewrite the "Auto-Install vs Explicit Install" ASCII diagram and its surrounding prose so the "managed launch / join" column lists all skills currently resolved from `managed_launch_sets`, the "explicit external install" column lists all skills resolved from `cli_default_sets`, and the totals match the resolved set contents (not a frozen "11 / 12" count).
- [ ] 2.4 Update the per-set expansion table (currently the `mailbox-core`, `mailbox-full`, `advanced-usage`, `touring`, `user-control`, `agent-instance`, `agent-messaging`, `agent-gateway` rows) so `user-control` explicitly lists `houmao-agent-loop-pairwise` and `houmao-agent-loop-relay`, matching the current `catalog.toml` contents.
- [ ] 2.5 Confirm the narrative routing row for `houmao-credential-mgr` still points operators at `houmao-mgr credentials <tool> ...` / `houmao-mgr project credentials <tool> ...` and does not drift back to the retired `project agents tools <tool> auth ...` path.

## 3. CLI Reference — credentials command group

- [ ] 3.1 Add a `### credentials — Dedicated credential management` section to `docs/reference/cli/houmao-mgr.md` under "Command Groups", placed in alphabetical or conceptual order alongside `admin`, `agents`, `brains`, `mailbox`, `project`, `server`, and `system-skills`.
- [ ] 3.2 In that new section, document the top-level `houmao-mgr credentials` family: synopsis, the three supported tool subcommands (`claude`, `codex`, `gemini`), the CRUD verbs (`list`, `get`, `add`, `set`, `remove`, `rename`), and the role of `--agent-def-dir <path>` as the plain-agent-definition-directory targeting flag.
- [ ] 3.3 Derive per-tool credential input coverage (for example `--api-key`, `--auth-token`, `--oauth-token`, `--config-dir`, `--base-url`, `--oauth-creds`, `--gemini-oauth-creds`) from live Click help output at `src/houmao/srv_ctrl/commands/credentials.py` and keep flag descriptions short but accurate.
- [ ] 3.4 Explain the `credentials` vs `project credentials` routing boundary: `project credentials <tool> ...` is the project-scoped wrapper that targets the active Houmao project overlay, while `credentials <tool> --agent-def-dir <path>` targets plain agent-definition directories. State that the two surfaces share semantics and that the project-scoped wrapper is preferred when an active overlay is present.
- [ ] 3.5 Cross-link the `houmao-credential-mgr` system skill section, the existing `project easy` credential-lane notes (`project credentials claude add|set` notes, the Claude/Gemini auth-bundle notes), and `docs/reference/claude-vendor-login-files.md`.
- [ ] 3.6 Add a "credentials" link to `docs/index.md` under "CLI Surfaces" pointing at the new `houmao-mgr.md` anchor so the docs landing page surfaces the command family.

## 4. CLI Reference — headless execution overrides on prompt surfaces

- [ ] 4.1 Expand the `agents prompt` row and accompanying prose inside `docs/reference/cli/houmao-mgr.md` so `--model`, `--reasoning-level`, TUI-target rejection, and the no-persistence contract are all explicitly documented. Use an option table similar to the existing `brains build` or `agents launch` tables where it improves scanability.
- [ ] 4.2 Confirm that `docs/reference/cli/agents-turn.md` and `docs/reference/cli/agents-gateway.md` already document `--model` and `--reasoning-level` for `agents turn submit` and `agents gateway prompt` respectively, and strengthen the wording where needed so the TUI-target rejection, no-persistence, and partial-override semantics are explicit on all three pages. Cross-link from the `houmao-mgr.md` `agents` section into those child pages.
- [ ] 4.3 Cross-check `docs/reference/managed_agent_api.md` against the `headless-execution-overrides` spec so the managed-agent HTTP routes (`POST /houmao/agents/{agent_ref}/turns`, `/gateway/control/prompt`, `/gateway/requests`) and direct gateway routes (`POST /v1/control/prompt`, `POST /v1/requests` for `submit_prompt`) describe the request-scoped `execution.model` payload, normalized reasoning level, TUI rejection semantics, and non-persistence. If `managed_agent_api.md` is missing the coverage, add it; if the coverage is already present, note that in the task result and leave it alone.
- [ ] 4.4 Ensure the operator-facing reference explicitly states the partial-override behavior: supplying `--reasoning-level` alone (without `--model`) merges with the launch-resolved model defaults through the shared headless resolution helper, and supplying neither leaves the launch-resolved defaults in effect.

## 5. Verification

- [ ] 5.1 Run `pixi run openspec validate refresh-docs-skills-credentials-headless --strict` and resolve any reported issues.
- [ ] 5.2 Re-read `README.md`, `docs/getting-started/system-skills-overview.md`, and `docs/reference/cli/houmao-mgr.md` end-to-end after the edits to catch any lingering "twelve" literals, frozen "11 / 12" set counts, references to the retired `project agents tools <tool> auth ...` credential path, or single-line `agents prompt` rows that hide the new override flags.
- [ ] 5.3 Grep the repository for `twelve` and `houmao-agent-instance` to verify no docs text still asserts the old count or frozen auto-install wording outside of openspec archives, and grep for `--model` / `--reasoning-level` across `docs/` to confirm that every documented prompt surface references both flags.
- [ ] 5.4 Build docs locally (for example `pixi run docs-serve` if available in the active environment) and smoke-check the updated pages render without broken anchors or stale cross-links.
- [ ] 5.5 Confirm that no code, test, or CLI surface was touched in this change — the diff should be restricted to markdown files under `README.md`, `docs/**`, and `openspec/changes/refresh-docs-skills-credentials-headless/**`.
