## REMOVED Requirements

### Requirement: Pack-owned agent assets use canonical layout only
**Reason**: The current `scripts/demo/houmao-server-agent-api-demo-pack/` workflow is being archived under `scripts/demo/legacy/` rather than preserved as a maintained demo-pack capability.
**Migration**: Preserve the archived pack as historical reference only. Define any redesigned direct managed-agent API workflow as a new supported capability later.
