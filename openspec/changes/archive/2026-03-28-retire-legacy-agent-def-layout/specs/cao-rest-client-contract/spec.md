## REMOVED Requirements

### Requirement: Live demo scripts prove end-to-end prompt processing with real providers
**Reason**: The repository is archiving the current `scripts/demo/*` surface under `scripts/demo/legacy/` and no longer wants live demo tutorial packs to define part of the maintained CAO REST client contract.
**Migration**: Keep the CAO REST client contract focused on supported runtime/client behavior. When redesigned demo workflows exist, specify them separately as new capabilities instead of preserving the current demo-pack requirement.
