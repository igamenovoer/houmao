## 1. README managed-header callout

- [x] 1.1 Rewrite the `> **Managed prompt header.**` blockquote at README.md line 349 to describe the five-section architecture (identity, houmao-runtime-guidance, automation-notice default on; task-reminder, mail-ack default off), the `--managed-header-section SECTION=enabled|disabled` per-launch override, and `--no-managed-header` whole-header opt-out. Keep it as a single blockquote paragraph with 3-4 sentences. Link to `docs/reference/run-phase/managed-prompt-header.md`.

## 2. Docs index entry

- [x] 2.1 Update the Managed Launch Prompt Header line in `docs/index.md` (line 46) to mention per-section control alongside the existing composition/opt-out description.
