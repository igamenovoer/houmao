## MODIFIED Requirements

### Requirement: Getting-started guide narrates the packaged system skills

The `houmao-specialist-mgr` description in `docs/getting-started/system-skills-overview.md` SHALL include the "set" (edit/patch) verb alongside create, list, inspect, and remove.

#### Scenario: Reader sees specialist editing in the system-skills overview

- **WHEN** a reader scans the system-skills overview skill table
- **THEN** the `houmao-specialist-mgr` row includes "set" in its verb list
