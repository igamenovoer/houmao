## 1. Semantic Baseline and Migration Map

- [x] 1.1 Inventory every file in the twenty current system-skill directories at Git commit `8f377c468bc7f87ff40dbf40c0a68327616112bd`, including entrypoints, metadata, executable pages, references, scripts, templates, assets, and examples.
- [x] 1.2 Extract a checked-in semantic-preservation inventory for each source skill covering trigger boundaries, help, public operations and aliases, default behavior, inputs, outputs, gates, blockers, evidence handoffs, target rules, side effects, and stop conditions.
- [x] 1.3 Record the fixed source-to-target mapping for sixteen shared children, two top-level loops, touring-to-welcome, and the specialist compatibility alias.
- [x] 1.4 Compare the pre-compaction sources with the current actor-aware implementation and mark only actor eligibility, immutable frame handling, target narrowing, and scanner-safe child entrypoints as intentional retained deltas.
- [x] 1.5 Record a reason for every source runtime file that changes ownership or is omitted, and resolve every unexplained semantic difference before editing final skill prose.
- [x] 1.6 Add source-derived test fixtures that do not require Git at test runtime and can verify preserved operations, aliases, gates, outputs, help posture, and stop conditions.

## 2. Static System-Skill Asset Tree

- [x] 2.1 Create `public/houmao-shared-routines/` as a complete standalone skill with `SKILL.md`, metadata, shared commands and references, and sixteen owned `SKILL-MAIN.md` child directories.
- [x] 2.2 Move the sixteen ordinary routine trees from the active protected source into shared-routines ownership without losing scripts, references, commands, assets, templates, or runtime metadata.
- [x] 2.3 Restore `public/houmao-agent-loop-pro/` as a complete standalone `SKILL.md` skill with all pro commands, references, scripts, scaffolds, and design resources owned locally.
- [x] 2.4 Restore `public/houmao-agent-loop-lite/` as a complete standalone `SKILL.md` skill with all lite commands, references, scripts, and scaffolds owned locally.
- [x] 2.5 Remove pro and lite from the shared-routines child inventory and update every shared or actor route to use the top-level loop siblings.
- [x] 2.6 Remove active admin and agent route variants, protected mount sources, and the now-empty active `protected/` tree without changing historical `legacy/` assets.
- [x] 2.7 Update all moved local links and cross-skill references so every local link resolves inside its standalone or parent-scoped owner and every external dependency uses a sibling skill invocation.
- [x] 2.8 Update package-data inclusion so wheels and sdists carry the six complete public roots, all sixteen `SKILL-MAIN.md` children, and no obsolete active protected composition source.

## 3. Imsight-Format Skill Rewrite and Semantic Preservation

- [x] 3.1 Rewrite `houmao-admin-welcome` in current Imsight format while preserving the full pre-compaction touring orientation, menus, guided paths, subsystem exploration, compact presentation, explanatory questions, and non-linear continuation behavior.
- [x] 3.2 Rewrite `houmao-admin-entrypoint` as a collection-of-routines mega router with a numbered workflow, immutable admin frame, target gate, complete eligible route table, sibling destinations, welcome delegation, and `DO NOT` guardrails.
- [x] 3.3 Rewrite `houmao-agent-entrypoint` as a collection-of-routines mega router with a numbered workflow, fresh self-verification, immutable agent frame, complete eligible route table, sibling destinations, and fail-closed guardrails.
- [x] 3.4 Write the public `houmao-shared-routines` router with direct-admin default posture, explicit `as-agent` verification, sixteen distinguishing `When to Route Here` rows, selective child loading, loop sibling handoffs, and direct-invocation guardrails.
- [x] 3.5 Format the `houmao-project-mgr` child from its pre-compaction source without changing its project boundaries, launcher order, inputs, outputs, actions, help, or guardrails.
- [x] 3.6 Format the `houmao-credential-mgr` child without changing credential lanes, secret handling, launcher order, operations, missing-input gates, provider boundaries, or output contracts.
- [x] 3.7 Format the `houmao-agent-definition` child without changing roles, recipes, launch dossiers, specialists, profiles, config drafts, fast-forward, easy launch or stop, credential boundaries, or lifecycle handoffs.
- [x] 3.8 Implement `specialist-mgr` as an admin and direct-shared compatibility route alias that preserves the original wrapper's explanation and delegates to agent-definition without owning separate commands.
- [x] 3.9 Format the `houmao-operator-messaging` child without changing manual activation, clarify-before-dispatch behavior, confirmation gate, prompt-versus-mail routing, or dispatch reporting.
- [x] 3.10 Format the `houmao-process-emails-via-gateway` child without changing prompt-provided bootstrap, metadata-first triage, continuation priority, reply hardening, archive-after-success, or stop-after-round behavior.
- [x] 3.11 Format the `houmao-agent-email-comms` child without changing resolver order, gateway and transport behavior, operation inventory, operator-origin posting, fallback rules, or mailbox output contracts.
- [x] 3.12 Format the `houmao-adv-usage-pattern` child without changing supported pattern selection, loop chooser boundaries, composed skill calls, or stop conditions.
- [x] 3.13 Format the `houmao-utils-workspace-mgr` child without changing plan, create, validate, summarize, workspace topology, repository safety, memo seed, or submodule behavior.
- [x] 3.14 Format the `houmao-ext-graphing` child without changing Plotly and Vega-Lite selection, schema discovery, validation, rendering, repair, output, or safety behavior.
- [x] 3.15 Format the `houmao-mailbox-mgr` child without changing root selection, account and binding operations, structural-versus-actor state, launcher behavior, or cleanup and export contracts.
- [x] 3.16 Format the `houmao-memory-mgr` child without changing its memo-trigger boundary, supported targets, read/write/remove operations, seed behavior, or memory ownership guardrails.
- [x] 3.17 Format the `houmao-agent-instance` child without changing launch, join, list, stop, relaunch, cleanup, target recovery, lifecycle boundaries, or joined-session transition contract.
- [x] 3.18 Format the `houmao-agent-inspect` child without changing its read-only posture, discovery, screen, mailbox, artifact, log, target, or evidence behavior.
- [x] 3.19 Format the `houmao-agent-messaging` child without changing discovery, prompt, interrupt, gateway queue, raw input, mail, reset-context, route preference, or target rules.
- [x] 3.20 Format the `houmao-agent-gateway` child without changing lifecycle, discovery, services, reminder, notifier, HTTP surface, targeting, or gateway-only boundaries.
- [x] 3.21 Format the `houmao-interop-ag-ui` child without changing validation, framing, rendering, publishing, protocol and implementation separation, examples, or safety boundaries.
- [x] 3.22 Rewrite `houmao-agent-loop-pro` with the Imsight complex-procedure subcommand flavor while preserving manual activation, upfront planning, no-operation `init`, `<loop-dir>` gate, every operation and alias, generated artifacts, execution stages, and constraints.
- [x] 3.23 Rewrite `houmao-agent-loop-lite` with the Imsight complex-procedure subcommand flavor while preserving manual activation, upfront planning, no-operation `init`, `<loop-dir>` gate, every operation and alias, Markdown/direct-SQL boundaries, execution stages, and constraints.
- [x] 3.24 Add a concise numbered workflow and native-planning fallback to every executable command page, keeping detailed domain procedure in the owning sections or references.
- [x] 3.25 Normalize every skill entrypoint's negative-action section to concise `DO NOT` guardrails without weakening or dropping source prohibitions, and move positive instructions out of guardrails.
- [x] 3.26 Add the standard `skill_invocation_notation` frontmatter value to every page using object designators and validate bare skill or subskill components plus fully parenthesized subcommand chains.
- [x] 3.27 Rewrite discovery descriptions to `Use when...` trigger language without broadening activation and add synchronized `agents/openai.yaml` policy, including narrow welcome implicit invocation and disabled implicit invocation for the other five roots.
- [x] 3.28 Run the Imsight format checklist across all six standalone roots, all sixteen children, and all executable command pages, then reconcile the semantic-preservation inventory before proceeding.

## 4. Sibling Routing and Actor Contracts

- [x] 4.1 Add static admin route entries for admin-only and shared children, both top-level loops, the specialist compatibility alias, help, and welcome commands with one distinguishing route sentence each.
- [x] 4.2 Add static agent route entries for the agent-only and shared children plus both top-level loops, and retain explicit rejection of admin-only routes.
- [x] 4.3 Define a sibling handoff frame contract that preserves actor kind, entrypoint name, verified identity when present, requested target, selected route, and selected operation without mutating actor identity.
- [x] 4.4 Implement direct shared-routines actor selection so no-frame invocation defaults to admin, explicit `as-agent` performs fresh verification, and inherited frames cannot be replaced by prompt text.
- [x] 4.5 Implement direct pro and lite actor selection with admin default, explicit managed-self verification, and inherited-frame preservation.
- [x] 4.6 Keep joined-session adoption as the only admin-to-agent transition, end the admin route after successful join, verify managed self, and hand subsequent work to the agent entrypoint.
- [x] 4.7 Replace every current reference to entrypoint-local `subskills/houmao-shared-routines`, protected route files, or nested loop children with the correct static sibling invocation.
- [x] 4.8 Validate the complete admin, agent, direct-admin, and direct-agent route matrices against child eligibility and runtime target boundaries.

## 5. V4 Manifest and Static Lifecycle

- [x] 5.1 Replace the v3 manifest schema with `houmao-system-skills.v4` records for six standalone skills, sixteen shared children, actor eligibility, commands and aliases, dependencies, activation posture, and pack membership.
- [x] 5.2 Update manifest parsing and models to remove protected mounts, audience route paths, shared mount paths, and composition-only fields.
- [x] 5.3 Add source validation for exactly six public `SKILL.md` roots, exactly sixteen shared `SKILL-MAIN.md` children, entrypoint-role ambiguity, complete local links, route uniqueness, dependency validity, metadata posture, and forbidden placeholders.
- [x] 5.4 Replace `compose_system_skill_pack()` and `_compose_protected_mount()` with a deduplicating static pack resolver and complete-directory staging API; remove obsolete exports and callers.
- [x] 5.5 Implement byte-preserving recursive copy staging and direct complete-directory symlink staging without hidden materialization roots.
- [x] 5.6 Introduce the new receipt schema with one standalone-skill record per destination and a non-empty owning-pack set, content digest, projection mode, and destination evidence.
- [x] 5.7 Update install and sync transactions to preflight all unique destinations, stage and validate the complete union, commit atomically, preserve unrelated paths, and write the receipt last.
- [x] 5.8 Update status inspection to classify static members and packs, validate shared child completeness, report owner sets, and identify v3 composed receipt drift.
- [x] 5.9 Implement transactional v3-to-v4 upgrade that replaces receipt-owned actor entrypoints, adds shared and loop siblings, removes obsolete owned materialization data after commit, and preserves unowned conflicts.
- [x] 5.10 Update uninstall to subtract selected pack ownership and remove a projection only after its final owner is removed.
- [x] 5.11 Enforce one projection mode per receipt-owned static collection and handle requested mode changes through explicit transactional replacement or preflight failure.
- [x] 5.12 Update managed system-skill policy and default resolution so external CLI defaults to admin and managed launch, rebuild, relaunch, and join default to agent while retaining supported policy modes.
- [x] 5.13 Update `houmao-mgr system-skills list` plain and structured output for six standalone skills, sixteen children, pack membership, shared ownership, default lanes, and activation posture.
- [x] 5.14 Update install, status, upgrade, and uninstall CLI input validation and output to use static members and owner sets and remove protected-mount or materialization terminology.
- [x] 5.15 Remove obsolete composition-state cleanup paths only after v3 migration behavior is covered, while retaining conservative legacy flat-skill classification.

## 6. Managed Runtime and Generated Prompts

- [x] 6.1 Update system-skill availability checks so mailbox and notifier prompt generation requires both `houmao-agent-entrypoint/SKILL.md` and `houmao-shared-routines/SKILL.md` in the target tool root.
- [x] 6.2 Rewrite generated mailbox command and notifier prompt text to invoke the public agent entrypoint and describe sibling shared delegation rather than protected traversal below the entrypoint.
- [x] 6.3 Preserve the existing API fallback when either required sibling is missing, incomplete, disabled, or unavailable.
- [x] 6.4 Update brain construction, reused-home synchronization, launch, rebuild, relaunch, and join integration to install the four static agent-pack siblings in copy mode.
- [x] 6.5 Update system-skill reference helpers and runtime prompt fixtures so loops resolve as top-level skills and ordinary routines resolve through the shared sibling.
- [x] 6.6 Keep `houmao-auto-system-prompt` separate from the v4 manifest, pack receipts, static public root, and external Skills CLI inventory.

## 7. Documentation and Migration Guidance

- [x] 7.1 Rewrite the README system-skills inventory and quick start around the six public roots, admin welcome, normal actor entrypoints, direct shared advanced use, and direct loop use.
- [x] 7.2 Add copy-paste and Skills CLI examples for listing all six, installing all six, installing the five-member admin surface, and installing the four-member agent surface without claiming dependency resolution.
- [x] 7.3 Rewrite `docs/getting-started/system-skills-overview.md` for static source identity, sibling routing, activation policy, sixteen shared children, actor posture, and installation choices.
- [x] 7.4 Rewrite `docs/reference/cli/system-skills.md` for v4 manifest records, shared owner sets, static lifecycle commands, v3 upgrade, conflicts, and direct public invocation.
- [x] 7.5 Update related mailbox, agent-definition, lifecycle, memory, workspace, loop, AG-UI, graphing, project, launch, and join docs to use sibling routes and top-level loops.
- [x] 7.6 Add breaking migration guidance from v3 composed actor packs and old flat skill prompts to the v4 static roots, including the preserved `specialist-mgr` alias and touring-to-welcome mapping.
- [x] 7.7 Audit documentation, examples, generated prompt fixtures, and tests for stale protected mounts, entrypoint-local shared paths, nested loop paths, composition placeholders, and claims that shared routines is not public.

## 8. Automated Coverage

- [x] 8.1 Add manifest and schema tests for exact six-root inventory, exact sixteen-child inventory, pack membership, activation posture, route uniqueness, dependency closure, and invalid obsolete fields.
- [x] 8.2 Add content validation for Imsight workflows, freeform fallbacks, subcommand flavor, `When to Route Here` guidance, role-canonical filenames, invocation notation, `DO NOT` guardrails, help posture, and metadata policy.
- [x] 8.3 Add semantic-preservation tests for every pre-compaction source skill using the checked-in operation, alias, default, gate, output, help, and stop-condition fixtures.
- [x] 8.4 Add static source completeness tests for local links, sibling dependency declarations, absence of entrypoint-local subskills, absence of active protected sources, and absence of source placeholders.
- [x] 8.5 Add copy byte-identity and direct-symlink target tests for admin, agent, and combined pack staging.
- [x] 8.6 Add receipt transaction tests for deduplicated both-pack install, owner addition, owner subtraction, last-owner removal, mode changes, rollback, drift, and unrelated-path preservation.
- [x] 8.7 Add v3-to-v4 upgrade tests for healthy composed packs, incomplete packs, modified conflicts, receipt-last commit, rollback restoration, and obsolete materialization cleanup.
- [x] 8.8 Update CLI smoke tests for static list, default and explicit install, status, upgrade, overlapping-owner uninstall, obsolete selector diagnostics, and structured output.
- [x] 8.9 Update managed runtime integration tests for four-member agent defaults, policy override and disablement, reused homes, role-authored skill preservation, and auto-skill separation.
- [x] 8.10 Add generated-prompt tests for both-siblings-present, entrypoint-only, shared-only, disabled, tool-specific native invocation, and API fallback cases.
- [x] 8.11 Add deterministic exact-`SKILL.md` discovery tests that return six names and exclude all nested `SKILL-MAIN.md` children.
- [x] 8.12 Add a Skills CLI smoke test using the local public source with `--list` and explicit `--skill` selections, isolating any actual installation under a temporary project directory.
- [x] 8.13 Update distribution tests to inspect wheel and sdist contents for all six roots, sixteen children, scripts and assets, and absence of active protected composition files.
- [x] 8.14 Update documentation contract tests so README, overview, CLI reference, manifest, route tables, and Skills CLI examples agree on names and pack membership.

## 9. Verification

- [x] 9.1 Run strict OpenSpec validation for this change and resolve every delta-operation, scenario, and artifact consistency error.
- [x] 9.2 Run the focused manifest, static asset, semantic-preservation, actor-routing, lifecycle, CLI, managed-home, mailbox-prompt, and documentation test suites.
- [x] 9.3 Run `pixi run format`, inspect every resulting skill and Python change, and confirm formatting did not alter semantic fixtures or unrelated user work.
- [x] 9.4 Run `pixi run lint` and resolve all change-related findings.
- [x] 9.5 Run `pixi run typecheck` and resolve all change-related strict typing failures.
- [x] 9.6 Run `pixi run test` and map every removed composer assertion to retained static source, staging, route, or receipt coverage.
- [x] 9.7 Run the relevant runtime-focused suites for managed launch, rebuild, relaunch, join, mailbox prompts, and reused-home synchronization.
- [x] 9.8 Run the local Skills CLI list and temporary copy-install smoke checks and verify exactly six discoverable roots plus complete admin and agent sibling selections.
- [x] 9.9 Run `pixi run build-dist` and `pixi run check-dist`, then inspect built artifacts for static source completeness.
- [x] 9.10 Search the repository for stale active composition APIs, protected mount terminology, nonexistent entrypoint-local shared paths, nested loop routes, and old generated-prompt claims; retain only explicit historical or migration references.
- [x] 9.11 Review the final diff against the semantic baseline and confirm that every intentional behavior change is limited to the approved packaging, actor, routing, and Imsight-structure decisions.
