## 1. Broaden automated project-aware coverage

- [x] 1.1 Add or extend automated tests for overlay-selection precedence, nearest-ancestor reuse, and `.git` file-or-directory worktree boundary handling for nested repositories.
- [x] 1.2 Add or extend automated tests for non-creating `houmao-mgr project status`, implicit bootstrap notices, and selected-root detail reporting across maintained project-aware command surfaces.
- [x] 1.3 Add or extend automated tests for overlay-local runtime or jobs or mailbox placement plus explicit shared-root override behavior across maintained build, launch, mailbox, cleanup, and server paths.

## 2. Validate representative maintained workflows

- [x] 2.1 Run representative maintained demo or operator workflows that now rely on overlay-local defaults, including the maintained minimal launch and single-agent mail wake-up surfaces or maintained equivalents.
- [x] 2.2 Record the executed validation commands and outcomes in the implementation notes or change summary so the project-aware close-out evidence is reproducible.

## 3. Close out the parent project-aware change

- [x] 3.1 Re-run the focused verification commands needed for the broadened matrix, including `ruff` and the targeted `pytest` coverage added by this change.
- [x] 3.2 Update `openspec/changes/make-operations-project-aware/tasks.md` to mark `5.2` and `5.3` complete once the automated and workflow validation passes.
- [x] 3.3 Re-run `openspec status --change make-operations-project-aware --json` or `openspec instructions apply --change make-operations-project-aware --json` and use the result as the parent readiness check.
