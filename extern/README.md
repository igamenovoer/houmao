# extern

## Purpose
Holds external code and repositories used by this workspace.

## Layout
- `tracked/`: Tracked external dependencies (for example, git submodules).
- `orphan/`: Local/untracked external checkouts or references.
