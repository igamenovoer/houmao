interface:
  display_name: "Houmao Agent Loop Pairwise"
  short_description: "Formulate and operate restored stable master-owned pairwise loop runs."
  default_prompt: "Use $houmao-agent-loop-pairwise only when the user explicitly requests it and needs to turn a goal into a pairwise loop plan or send `start`, `status`, or `stop` control requests to a designated Houmao master without making the user agent part of the execution loop."
