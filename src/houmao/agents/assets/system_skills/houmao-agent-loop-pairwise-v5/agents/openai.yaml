interface:
  display_name: "Houmao Agent Loop Pairwise"
  short_description: "Manually init loop intention, clarify intent, generate or update execplans, and operate generated loops."
  default_prompt: "Use $houmao-agent-loop-pairwise-v5 only when the user explicitly requests it or names a supported loop operation. If invoked without another operation or prompt, treat it as init: ask for the output loop directory and scaffold editable loop-dir/intention material only after the user provides it. Generate or update loop-dir/execplan from intention source using flexible defaults for manifest-indexed specs, role/event/tick skills, agent bindings, harness services, workspace contracts, communication contracts, compact runtime bookkeeping, and run artifacts. Keep task-specific objective, topology, policy, evidence, and domain records derived from intention or accepted clarification decisions. Route workspace creation, mailbox work, gateway work, managed-agent lifecycle, memory, and inspection through maintained Houmao skills."
