# Execplan Package: ${loop_dir_name}

This directory contains generated operational material derived from `../intention/`.

## Generated Posture

- Regenerate or update this package from source intent instead of treating it as the primary editable source.
- Use `manifest.toml` as the package index for this revision.
- Use `docs/` as human support material and `specs/`, `harness/`, `skills/`, and `agents/` as the generated operational surfaces.

## Package Areas

- `specs/`
- `skills/`
- `agents/`
- `harness/`
- `docs/`

## Scaffold Status

- Generated by scaffold profile `${scaffold_profile}` on `${generated_date}`.
- Final support docs should be populated from the authoritative generated artifacts during finalization.
