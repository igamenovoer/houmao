interface:
  display_name: "Houmao Agent Loop Relay"
  short_description: "Formulate and operate master-owned relay loop runs."
  default_prompt: "Use $houmao-agent-loop-relay when a user-controlled agent needs to turn a goal into a relay loop plan or send start, status, or stop control requests to a designated Houmao master or loop origin without making the user agent part of the execution loop."
