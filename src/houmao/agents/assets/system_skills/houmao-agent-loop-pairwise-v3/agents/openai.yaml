interface:
  display_name: "Houmao Agent Loop Pairwise V3"
  short_description: "Author, initialize, and operate workspace-aware enriched master-owned pairwise loop runs."
  default_prompt: "Use $houmao-agent-loop-pairwise-v3 only when the user explicitly requests it and needs to turn a goal into a workspace-aware enriched pairwise loop plan written under a user-selected output directory, choose a `standard` or `custom` workspace contract, run canonical `initialize` through default precomputed routing-packet validation plus launch-profile mailbox-association precheck, launch-profile-backed participant launch when provided, fail-closed email/mailbox verification, run-owned memo materialization, and when explicit operator preparation-wave is selected, required gateway mail-notifier verification or enablement before any preparation mail, or send compact memo-read `start` by mail by default unless the user explicitly requests direct prompt delivery, plus `peek`, `ping`, `pause`, `resume`, `recover_and_continue`, `stop`, or `hard-kill` control requests without making the user agent part of the execution loop."
