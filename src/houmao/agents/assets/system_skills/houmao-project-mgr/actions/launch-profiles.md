# Explicit Launch Profiles Have Moved

This file is retained only as a routing note for older installed copies.

Use `houmao-agent-definition/subskills/low-level/launch-profiles.md` for explicit recipe-backed launch-profile authoring through `project agents launch-profiles list|get|add|set|remove`.

Do not run launch-profile authoring from `houmao-project-mgr`.
