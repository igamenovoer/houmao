interface:
  display_name: "Houmao Agent Loop Tree"
  short_description: "Formulate and operate restored stable master-owned tree loop runs."
  default_prompt: "Use $houmao-agent-loop-pairwise only when the user explicitly requests it and needs to turn a goal into a tree loop plan or send `start`, `status`, or `stop` control requests to a designated Houmao master without making the user agent part of the execution loop."
