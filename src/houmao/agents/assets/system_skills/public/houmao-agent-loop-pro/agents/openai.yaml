interface:
  display_name: "Houmao Agent Loop Pro"
  short_description: "Author and operate topology-aware pro Houmao loops."
  default_prompt: "Use $houmao-agent-loop-pro only when explicitly requested. If no operation is present, select init and ask for <loop-dir> before creating files."
policy:
  allow_implicit_invocation: false
