interface:
  display_name: "Houmao Agent Entrypoint"
  short_description: "Verified managed-agent execution entrypoint"
  default_prompt: "Use $houmao-agent-entrypoint for a concrete managed-agent task, freshly verify self, and delegate ordinary work to $houmao-shared-routines or explicit loop work to the top-level loop sibling."
policy:
  allow_implicit_invocation: false
