interface:
  display_name: "Houmao Agent Entrypoint"
  short_description: "Verified managed-agent execution entrypoint"
  default_prompt: "Use $houmao-agent-entrypoint, verify managed self before each substantive route, preserve the immutable agent actor frame, and let each parent explicitly load only the selected protected SKILL-MAIN.md entrypoint."
