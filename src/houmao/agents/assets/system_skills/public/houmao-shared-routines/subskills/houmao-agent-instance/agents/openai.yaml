interface:
  display_name: "Houmao Manage Agent Instance"
  short_description: "Manage live Houmao agents and definition-owned state."
  default_prompt: "Use $houmao-shared-routines agent-instance to manage lifecycle, typed runtime variables, named mindsets, and definition-owned private workspaces through actor-scoped Houmao commands."
policy:
  allow_implicit_invocation: false
