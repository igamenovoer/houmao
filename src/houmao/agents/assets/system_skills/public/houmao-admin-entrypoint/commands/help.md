# Help

Explain that this entrypoint executes work for a human operator, requires explicit targets for target-sensitive operations, and routes through its nested `houmao-shared-routines` composition. Protected routine names are route arguments, not `$skill` invocations.
