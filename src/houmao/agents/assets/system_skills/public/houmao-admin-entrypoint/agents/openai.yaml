interface:
  display_name: "Houmao Admin Entrypoint"
  short_description: "Execute Houmao operations for a human operator"
  default_prompt: "Use $houmao-admin-entrypoint, establish the immutable admin actor frame, resolve explicit targets, and let each parent explicitly load only the selected protected SKILL-MAIN.md entrypoint."
