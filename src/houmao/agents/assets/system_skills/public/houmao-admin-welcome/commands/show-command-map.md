# Show Command Map

Show only public invocations. Use `$houmao-admin-welcome <command>` for orientation and `$houmao-admin-entrypoint <route> <command>` for work. Explain that route segments such as `agent-inspect`, `credential-mgr`, and `mailbox-mgr` are protected members nested under the entrypoint, not standalone installed skills.
