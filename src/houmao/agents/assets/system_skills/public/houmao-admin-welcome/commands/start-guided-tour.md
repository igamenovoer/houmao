# Start Guided Tour

Read [../references/orientation.md](../references/orientation.md). Inspect only enough state to distinguish a blank workspace, an existing Houmao project, running managed agents, and loop artifacts. Present current posture, the likely path, and the state-appropriate choices. Keep tables to four columns or fewer and use `more detail` for optional depth. Load [../references/guided-paths.md](../references/guided-paths.md) only after the user chooses a path.
