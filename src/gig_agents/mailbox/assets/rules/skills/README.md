# Mailbox Helper Skills

This directory is reserved for mailbox-local helper skills that may be published alongside the shared mailbox.

Runtime-owned mailbox system skills are projected into agent homes separately. This `rules/skills/` directory exists so a shared mailbox can carry mailbox-local helper materials without changing the runtime-owned skill namespace.
